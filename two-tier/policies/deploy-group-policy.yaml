apiVersion: v1
kind: policy
metadata:
  name: group-policy
spec:
  selector: group=='production' && app == 'database'
  ingress:
  - action: deny
    source:
      selector: group!='production'
  egress:
  - action: deny
    source:
      selector: group!='production'
