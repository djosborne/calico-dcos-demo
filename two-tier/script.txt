

0. prereqs
m1:
docker pull etcd

a's:
docker pull calico/calico-dcos:v0.3.1
docker pull calico/node:v0.22.0
docker pull calico/node-libnetwork:v0.9.0

etcdctl set /calico/v1/policy/tier/default/metadata '{"order": 0}'



1. install etcd
	docker run --detach \
		--net=host \
		--name calico-etcd quay.io/coreos/etcd:v2.0.11 \
		--advertise-client-urls "http://m1.dcos:2379" \
		--listen-client-urls "http://m1.dcos:2379,http://127.0.0.1:2379"



1. install calico from universe
check what it did:
node:

# Binaries
/opt/mesosphere/etc/dcos/network/cni

# Configuration
/opt/mesosphere/active/cni/

2. calicoctl pool

2. launch group-a app
3. launch group-b app
4. policies

docker run --rm --net=calico redis:alpine redis-cli -h database.marathon.containerip.dcos.thisdcos.directory -p 6379 SET views 0


frontend.marathon.containerip.dcos.thisdcos.directory
